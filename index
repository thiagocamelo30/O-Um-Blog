<!DOCTYPE html>
<html lang="pt-BR" data-theme="parchment" data-card="classico" data-ads="on">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>O Um Blog — Crônicas da Terra-média</title>
<script>
  /* Aplica preferências antes da pintura para evitar flash de tema */
  (function () {
    var r = document.documentElement;
    try {
      r.dataset.theme = localStorage.getItem('oub-theme') || 'parchment';
      r.dataset.card = localStorage.getItem('oub-card') || 'classico';
      r.dataset.ads = localStorage.getItem('oub-ads') || 'on';
    } catch (e) {}
  })();
</script>
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,500;0,600;0,700;1,500;1,600&family=Mulish:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
<style>
  /* ===================== TOKENS ===================== */
  :root {
    --bg: #ece7d0;
    --bg-2: #e7d9bf;
    --surface: #faf4e7;
    --surface-2: #f3ead8;
    --ink: #2a2417;
    --ink-soft: #574d39;
    --muted: #8c7f64;
    --gold: #927121;
    --gold-bright: #b58f33;
    --gold-soft: rgba(146, 113, 33, 0.14);
    --green: #2f5135;
    --green-deep: #233f29;
    --green-soft: rgba(47, 81, 53, 0.12);
    --green-mid: rgba(47, 81, 53, 0.24);
    --forest: #21392b;
    --forest-line: rgba(241, 236, 221, 0.15);
    --line: rgba(42, 36, 23, 0.16);
    --line-soft: rgba(42, 36, 23, 0.09);
    --shadow: 0 1px 2px rgba(42, 36, 23, 0.05), 0 18px 40px -24px rgba(42, 36, 23, 0.35);
    --shadow-lift: 0 2px 4px rgba(42, 36, 23, 0.06), 0 30px 60px -28px rgba(42, 36, 23, 0.5);
    --ph-stripe: rgba(42, 36, 23, 0.06);
    --ph-base: #e9dcc2;
  }
  [data-theme="noturno"] {
    --bg: #14120d;
    --bg-2: #100e0a;
    --surface: #211d15;
    --surface-2: #1a160f;
    --ink: #ece2cd;
    --ink-soft: #c4b699;
    --muted: #8f8266;
    --gold: #d4ad5c;
    --gold-bright: #e6c378;
    --gold-soft: rgba(212, 173, 92, 0.14);
    --green: #80a880;
    --green-deep: #6b9170;
    --green-soft: rgba(128, 168, 128, 0.13);
    --green-mid: rgba(128, 168, 128, 0.26);
    --forest: #15271c;
    --forest-line: rgba(236, 226, 205, 0.12);
    --line: rgba(236, 226, 205, 0.14);
    --line-soft: rgba(236, 226, 205, 0.08);
    --shadow: 0 1px 2px rgba(0, 0, 0, 0.4), 0 18px 40px -24px rgba(0, 0, 0, 0.8);
    --shadow-lift: 0 2px 4px rgba(0, 0, 0, 0.5), 0 30px 60px -26px rgba(0, 0, 0, 0.9);
    --ph-stripe: rgba(236, 226, 205, 0.05);
    --ph-base: #1b1710;
  }

  * { box-sizing: border-box; }
  html { scroll-behavior: smooth; }
  body {
    margin: 0;
    background: var(--bg);
    color: var(--ink);
    font-family: 'Mulish', system-ui, sans-serif;
    font-size: 17px;
    line-height: 1.65;
    -webkit-font-smoothing: antialiased;
    transition: background 0.4s ease, color 0.4s ease;
  }
  h1, h2, h3, h4 {
    font-family: 'Playfair Display', Georgia, serif;
    font-weight: 600;
    line-height: 1.12;
    margin: 0;
    letter-spacing: -0.01em;
  }
  a { color: inherit; text-decoration: none; }
  ::selection { background: var(--gold-soft); }
  .wrap { width: min(1180px, 92vw); margin: 0 auto; }

  .eyebrow {
    font-family: 'Mulish', sans-serif;
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--green);
  }

  /* ===================== PLACEHOLDERS ===================== */
  .ph {
    position: relative;
    background-color: var(--ph-base);
    background-image: repeating-linear-gradient(
      135deg,
      transparent 0,
      transparent 11px,
      var(--ph-stripe) 11px,
      var(--ph-stripe) 12px
    );
    display: grid;
    place-items: center;
    overflow: hidden;
  }
  .ph__tag {
    font-family: 'Courier New', monospace;
    font-size: 10.5px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--muted);
    border: 1px dashed var(--line);
    background: var(--surface);
    padding: 6px 11px;
    border-radius: 3px;
  }

  /* ===================== HEADER ===================== */
  .site-header {
    position: sticky;
    top: 0;
    z-index: 50;
    background: color-mix(in srgb, var(--bg) 86%, transparent);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid transparent;
    transition: border-color 0.3s ease, box-shadow 0.3s ease, background 0.4s ease;
  }
  .site-header.scrolled {
    border-bottom-color: var(--line-soft);
    box-shadow: 0 10px 30px -26px rgba(42, 36, 23, 0.6);
  }
  .nav {
    display: flex;
    align-items: center;
    gap: 28px;
    height: 76px;
  }
  .brand { display: flex; align-items: center; gap: 13px; margin-right: auto; }
  .brand__ring {
    width: 38px; height: 38px; flex: none;
    border-radius: 50%;
    border: 2px solid var(--gold);
    position: relative;
    display: grid; place-items: center;
  }
  .brand__ring::after {
    content: "";
    width: 20px; height: 20px;
    border-radius: 50%;
    border: 1px solid var(--gold-bright);
    opacity: 0.55;
  }
  .brand__txt { display: flex; flex-direction: column; line-height: 1; }
  .brand__name {
    font-family: 'Playfair Display', serif;
    font-style: italic;
    font-weight: 600;
    font-size: 23px;
    color: var(--ink);
  }
  .brand__sub {
    font-size: 9.5px;
    letter-spacing: 0.26em;
    text-transform: uppercase;
    color: var(--muted);
    margin-top: 4px;
  }

  .menu { display: flex; align-items: center; gap: 4px; }
  .menu a {
    position: relative;
    font-size: 14.5px;
    font-weight: 600;
    color: var(--ink-soft);
    padding: 9px 14px;
    border-radius: 7px;
    transition: color 0.2s ease, background 0.2s ease;
  }
  .menu a::after {
    content: "";
    position: absolute;
    left: 14px; right: 14px; bottom: 4px;
    height: 1.5px;
    background: var(--green);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.28s cubic-bezier(0.4, 0, 0.1, 1);
  }
  .menu a:hover { color: var(--ink); }
  .menu a:hover::after, .menu a.active::after { transform: scaleX(1); }
  .menu a.active { color: var(--ink); }

  .search {
    display: flex; align-items: center; gap: 9px;
    background: var(--surface-2);
    border: 1px solid var(--line-soft);
    border-radius: 999px;
    padding: 8px 15px;
    width: 192px;
    transition: width 0.32s cubic-bezier(0.4,0,0.1,1), border-color 0.2s ease, box-shadow 0.2s ease, background 0.3s ease;
  }
  .search:focus-within {
    width: 248px;
    border-color: var(--green);
    box-shadow: 0 0 0 4px var(--green-soft);
  }
  .search svg { width: 16px; height: 16px; stroke: var(--muted); flex: none; }
  .search input {
    border: none; background: none; outline: none;
    font-family: inherit; font-size: 13.5px; color: var(--ink);
    width: 100%;
  }
  .search input::placeholder { color: var(--muted); }

  .hamburger {
    display: none;
    width: 42px; height: 42px;
    border: 1px solid var(--line);
    border-radius: 9px;
    background: var(--surface-2);
    cursor: pointer;
    align-items: center; justify-content: center;
    flex-direction: column; gap: 4px;
  }
  .hamburger span { width: 17px; height: 1.8px; background: var(--ink); transition: 0.25s; }

  /* ===================== BUTTONS ===================== */
  .btn {
    display: inline-flex; align-items: center; gap: 9px;
    font-family: 'Mulish', sans-serif;
    font-weight: 700;
    font-size: 14.5px;
    letter-spacing: 0.01em;
    padding: 13px 24px;
    border-radius: 9px;
    cursor: pointer;
    border: 1px solid transparent;
    transition: transform 0.18s ease, box-shadow 0.2s ease, background 0.2s ease, color 0.2s ease;
    white-space: nowrap;
  }
  .btn svg { width: 16px; height: 16px; }
  .btn--gold {
    background: linear-gradient(180deg, var(--gold-bright), var(--gold));
    color: #fff5e0;
    box-shadow: 0 1px 0 rgba(255,255,255,0.25) inset, 0 10px 24px -12px var(--gold);
  }
  [data-theme="noturno"] .btn--gold { color: #241b07; }
  .btn--gold:hover { transform: translateY(-2px); box-shadow: 0 1px 0 rgba(255,255,255,0.3) inset, 0 16px 30px -12px var(--gold); }
  .btn--ghost {
    background: transparent;
    color: var(--ink);
    border-color: var(--line);
  }
  .btn--ghost:hover { border-color: var(--green); color: var(--green); transform: translateY(-2px); }
  .btn--green {
    background: var(--green);
    color: #f3efe2;
  }
  [data-theme="noturno"] .btn--green { color: #14241a; }
  .btn--green:hover { transform: translateY(-2px); box-shadow: 0 16px 30px -14px var(--green); }

  /* ===================== HERO ===================== */
  .hero { position: relative; padding: 56px 0 60px; }
  .hero__grid {
    display: grid;
    grid-template-columns: 1.05fr 0.95fr;
    gap: 52px;
    align-items: center;
  }
  .hero__eyebrow { display: flex; align-items: center; gap: 12px; margin-bottom: 22px; }
  .hero__eyebrow::after { content: ""; height: 1px; width: 54px; background: var(--green); opacity: 0.7; }
  .hero h1 {
    font-size: clamp(40px, 5vw, 62px);
    margin-bottom: 22px;
  }
  .hero h1 em { font-style: italic; color: var(--gold); }
  .hero__dek {
    font-size: 18.5px;
    color: var(--ink-soft);
    max-width: 30em;
    margin-bottom: 28px;
    text-wrap: pretty;
  }
  .hero__meta {
    display: flex; align-items: center; gap: 14px;
    font-size: 13px; color: var(--muted);
    margin-bottom: 30px;
  }
  .hero__meta .dot { width: 3px; height: 3px; border-radius: 50%; background: var(--muted); }
  .hero__actions { display: flex; flex-wrap: wrap; gap: 14px; }

  .hero__media {
    position: relative;
    aspect-ratio: 4 / 4.4;
    border-radius: 16px;
    border: 1px solid var(--line);
    box-shadow: var(--shadow);
    background-image:
      repeating-linear-gradient(135deg, transparent 0, transparent 13px, var(--ph-stripe) 13px, var(--ph-stripe) 14px);
    background-color: var(--ph-base);
  }
  /* Animated spotlights (adapted from framer-motion SpotlightBackground) */
  .hero__media .spot {
    position: absolute; top: 50%; left: 50%;
    width: 86%; height: 86%;
    border-radius: 50%;
    filter: blur(46px);
    pointer-events: none;
    will-change: transform;
    mix-blend-mode: multiply;
  }
  [data-theme="noturno"] .hero__media .spot { mix-blend-mode: screen; }
  .spot--1 { background: radial-gradient(circle at center, var(--gold-bright), transparent 62%); opacity: 0.5; animation: spotDriftA 12s ease-in-out infinite; }
  .spot--2 { background: radial-gradient(circle at center, var(--green), transparent 62%); opacity: 0.46; animation: spotDriftB 15s ease-in-out infinite; animation-delay: -3s; }
  .spot--3 { background: radial-gradient(circle at center, var(--gold), transparent 62%); opacity: 0.42; animation: spotDriftC 18s ease-in-out infinite; animation-delay: -5s; }
  [data-theme="noturno"] .spot--1 { opacity: 0.62; }
  [data-theme="noturno"] .spot--2 { opacity: 0.58; }
  [data-theme="noturno"] .spot--3 { opacity: 0.55; }
  @keyframes spotDriftA {
    0%   { transform: translate(-50%, -50%) rotate(0deg); }
    35%  { transform: translate(-30%, -70%) rotate(15deg); }
    70%  { transform: translate(-70%, -30%) rotate(-15deg); }
    100% { transform: translate(-50%, -50%) rotate(0deg); }
  }
  @keyframes spotDriftB {
    0%   { transform: translate(-50%, -50%) rotate(-20deg); }
    35%  { transform: translate(-30%, -22%) rotate(0deg); }
    70%  { transform: translate(-70%, -42%) rotate(20deg); }
    100% { transform: translate(-50%, -50%) rotate(-20deg); }
  }
  @keyframes spotDriftC {
    0%   { transform: translate(-50%, -50%) rotate(10deg); }
    35%  { transform: translate(-80%, -68%) rotate(-10deg); }
    70%  { transform: translate(-40%, -32%) rotate(25deg); }
    100% { transform: translate(-50%, -50%) rotate(10deg); }
  }
  @media (prefers-reduced-motion: reduce) {
    .hero__media .spot { animation: none !important; }
  }
  .hero__media .ph__tag { position: absolute; bottom: 16px; left: 16px; }
  .hero__badge {
    position: absolute; top: 18px; right: 18px;
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: 999px;
    padding: 7px 15px;
    font-size: 12px; font-weight: 700;
    color: var(--green);
    display: flex; align-items: center; gap: 7px;
    box-shadow: var(--shadow);
  }
  .hero__badge .star { color: var(--gold); }

  /* ===================== AD BLOCKS ===================== */
  [data-ads="off"] .ad-block { display: none; }
  .ad-block {
    border: 1px dashed var(--line);
    border-radius: 12px;
    background:
      repeating-linear-gradient(135deg, transparent 0, transparent 13px, var(--ph-stripe) 13px, var(--ph-stripe) 14px);
    background-color: var(--surface-2);
    display: grid; place-items: center;
    text-align: center;
    padding: 26px;
  }
  .ad-block--leaderboard { min-height: 132px; margin: 8px 0 4px; }
  .ad-block--inline { min-height: 110px; }
  .ad-block__label {
    font-family: 'Courier New', monospace;
    font-size: 10px; letter-spacing: 0.2em; text-transform: uppercase;
    color: var(--muted);
  }
  .ad-block__hint {
    font-family: 'Courier New', monospace;
    font-size: 12px; color: var(--ink-soft); margin-top: 7px;
  }

  /* ===================== SECTIONS ===================== */
  section { scroll-margin-top: 90px; }
  .section-head {
    display: flex; align-items: flex-end; justify-content: space-between;
    gap: 20px; margin-bottom: 30px;
  }
  .section-head h2 { font-size: clamp(28px, 3.4vw, 38px); }
  .section-head p { color: var(--muted); font-size: 15px; margin: 8px 0 0; }
  .section-head a.more {
    font-size: 14px; font-weight: 700; color: var(--green);
    display: inline-flex; align-items: center; gap: 7px; white-space: nowrap;
  }
  .section-head a.more:hover { gap: 11px; }

  .posts {
    padding: 30px 0 56px;
  }
  .posts-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 26px;
  }

  /* ---- CARD (base / Clássico) ---- */
  .card {
    background: var(--surface);
    border: 1px solid var(--line-soft);
    border-radius: 14px;
    overflow: hidden;
    display: flex; flex-direction: column;
    box-shadow: var(--shadow);
    transition: transform 0.28s cubic-bezier(0.4,0,0.1,1), box-shadow 0.28s ease, border-color 0.28s ease;
  }
  .card:hover { transform: translateY(-6px); box-shadow: var(--shadow-lift); border-color: var(--line); }
  .card__media { aspect-ratio: 16 / 10; }
  .card__body { padding: 22px 22px 24px; display: flex; flex-direction: column; flex: 1; }
  .tag {
    align-self: flex-start;
    font-size: 11px; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase;
    color: var(--green);
    background: var(--green-soft);
    padding: 5px 11px; border-radius: 999px;
    margin-bottom: 14px;
  }
  .tag--gold { color: var(--gold); background: var(--gold-soft); }
  .card h3 { font-size: 22px; margin-bottom: 11px; transition: color 0.2s ease; }
  .card:hover h3 { color: var(--green); }
  .card__excerpt { color: var(--ink-soft); font-size: 14.5px; flex: 1; text-wrap: pretty; }
  .card__foot {
    display: flex; align-items: center; justify-content: space-between;
    margin-top: 18px; padding-top: 16px; border-top: 1px solid var(--line-soft);
    font-size: 12.5px; color: var(--muted);
  }
  .card__more { font-weight: 700; color: var(--green); display: inline-flex; gap: 6px; align-items: center; }
  .card:hover .card__more { gap: 10px; }

  /* ---- CARD variation: Pergaminho ---- */
  [data-card="pergaminho"] .posts-grid { gap: 30px; }
  [data-card="pergaminho"] .card {
    background: var(--surface-2);
    border: 1px solid var(--gold);
    border-radius: 4px;
    box-shadow: 0 0 0 1px var(--surface-2), 0 0 0 4px color-mix(in srgb, var(--gold) 22%, transparent), var(--shadow);
    padding: 11px;
  }
  [data-card="pergaminho"] .card:hover { transform: translateY(-6px); }
  [data-card="pergaminho"] .card__media {
    border-radius: 2px;
    aspect-ratio: 16 / 9;
    border: 1px solid var(--line);
  }
  [data-card="pergaminho"] .card__body {
    padding: 22px 14px 14px;
    text-align: center;
    align-items: center;
  }
  [data-card="pergaminho"] .tag {
    align-self: center;
    background: none;
    padding: 0;
    color: var(--gold);
    display: flex; align-items: center; gap: 9px;
  }
  [data-card="pergaminho"] .tag::before,
  [data-card="pergaminho"] .tag::after {
    content: ""; width: 16px; height: 1px; background: var(--gold); opacity: 0.6;
  }
  [data-card="pergaminho"] .card h3 { font-style: italic; font-size: 23px; }
  [data-card="pergaminho"] .card__excerpt { font-size: 14px; }
  [data-card="pergaminho"] .card__foot { width: 100%; justify-content: center; gap: 14px; border-top-color: var(--line); }
  [data-card="pergaminho"] .card__foot .card__more { color: var(--gold); }

  /* ===================== AFFILIATE ===================== */
  .affiliate { padding: 18px 0 64px; }
  .affiliate__inner {
    position: relative;
    border-radius: 20px;
    overflow: hidden;
    background: var(--green-deep);
    color: #f1ecdd;
    display: grid;
    grid-template-columns: 1fr 0.85fr;
    gap: 0;
    box-shadow: var(--shadow-lift);
    isolation: isolate;
  }
  .affiliate__inner::before {
    content: "";
    position: absolute; inset: 0; z-index: -1;
    background:
      radial-gradient(80% 120% at 100% 0%, rgba(212,173,92,0.22), transparent 55%),
      repeating-linear-gradient(135deg, transparent 0, transparent 16px, rgba(255,255,255,0.025) 16px, rgba(255,255,255,0.025) 17px);
  }
  .affiliate__text { padding: 52px 48px; }
  .affiliate__eyebrow {
    font-size: 12px; font-weight: 700; letter-spacing: 0.22em; text-transform: uppercase;
    color: var(--gold-bright); display: flex; align-items: center; gap: 11px; margin-bottom: 18px;
  }
  .affiliate__eyebrow::before { content: ""; width: 30px; height: 1px; background: var(--gold-bright); }
  .affiliate h2 { color: #fbf6e9; font-size: clamp(30px, 3.6vw, 42px); margin-bottom: 16px; }
  .affiliate h2 em { font-style: italic; color: var(--gold-bright); }
  .affiliate__text p { color: rgba(241,236,221,0.82); font-size: 16px; max-width: 34em; margin: 0 0 26px; text-wrap: pretty; }
  .affiliate__perks { display: flex; flex-wrap: wrap; gap: 9px; margin-bottom: 30px; }
  .affiliate__perks span {
    font-size: 12.5px; font-weight: 600;
    background: rgba(255,255,255,0.07);
    border: 1px solid rgba(255,255,255,0.13);
    border-radius: 999px; padding: 6px 13px;
    color: #ece3cf;
  }
  .affiliate__actions { display: flex; align-items: center; gap: 18px; flex-wrap: wrap; }
  .affiliate__disclosure { font-size: 11.5px; color: rgba(241,236,221,0.55); }
  .affiliate__media {
    position: relative;
    background-image:
      radial-gradient(90% 90% at 50% 30%, rgba(212,173,92,0.25), transparent 60%),
      repeating-linear-gradient(135deg, transparent 0, transparent 13px, rgba(255,255,255,0.04) 13px, rgba(255,255,255,0.04) 14px);
    background-color: rgba(0,0,0,0.18);
    display: grid; place-items: center;
    border-left: 1px solid rgba(255,255,255,0.08);
  }
  .affiliate__media .ph__tag { background: rgba(0,0,0,0.35); color: #d9cfb6; border-color: rgba(255,255,255,0.2); }

  /* ===================== FOOTER ===================== */
  .site-footer {
    background: var(--forest);
    color: #ece3cf;
    border-top: 3px solid var(--gold);
    padding: 60px 0 32px;
    transition: background 0.4s ease;
  }
  .site-footer .brand__name { color: #f6f1e4; }
  .site-footer .brand__sub { color: rgba(236, 227, 207, 0.6); }
  .footer-grid {
    display: grid;
    grid-template-columns: 1.6fr 1fr 1fr 1fr;
    gap: 36px;
    padding-bottom: 40px;
    border-bottom: 1px solid var(--forest-line);
  }
  .footer-brand .brand { margin-bottom: 16px; }
  .footer-brand p { color: rgba(236, 227, 207, 0.72); font-size: 14px; max-width: 30em; margin: 0 0 18px; }
  .socials { display: flex; gap: 10px; }
  .socials a {
    width: 38px; height: 38px; border-radius: 9px;
    border: 1px solid var(--forest-line);
    display: grid; place-items: center;
    color: rgba(236, 227, 207, 0.82);
    transition: 0.22s;
  }
  .socials a:hover { border-color: var(--gold-bright); color: var(--gold-bright); transform: translateY(-2px); background: rgba(255, 255, 255, 0.05); }
  .socials svg { width: 17px; height: 17px; }
  .footer-col h4 {
    font-family: 'Mulish', sans-serif; font-size: 12px; font-weight: 700;
    letter-spacing: 0.16em; text-transform: uppercase; color: var(--gold-bright);
    margin-bottom: 16px;
  }
  .footer-col ul { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 11px; }
  .footer-col a { font-size: 14.5px; color: rgba(236, 227, 207, 0.8); transition: color 0.2s; }
  .footer-col a:hover { color: var(--gold-bright); }
  .footer-bottom {
    display: flex; align-items: center; justify-content: space-between;
    gap: 16px; padding-top: 22px; flex-wrap: wrap;
    font-size: 12.5px; color: rgba(236, 227, 207, 0.55);
  }
  .footer-bottom .legal { max-width: 46em; }

  /* ===================== RESPONSIVE ===================== */
  @media (max-width: 980px) {
    .hero__grid { grid-template-columns: 1fr; gap: 36px; }
    .hero__media { aspect-ratio: 16 / 10; max-width: 560px; }
    .posts-grid { grid-template-columns: 1fr 1fr; }
    .affiliate__inner { grid-template-columns: 1fr; }
    .affiliate__media { min-height: 200px; border-left: none; border-top: 1px solid rgba(255,255,255,0.08); }
    .footer-grid { grid-template-columns: 1fr 1fr; }
  }
  @media (max-width: 720px) {
    body { font-size: 16px; }
    .menu, .search { display: none; }
    .hamburger { display: flex; }
    .nav { height: 66px; }
    .menu.open {
      display: flex; flex-direction: column; gap: 2px;
      position: absolute; top: 66px; left: 0; right: 0;
      background: var(--surface); border-bottom: 1px solid var(--line);
      padding: 12px 4vw 18px; box-shadow: var(--shadow);
    }
    .menu.open a { padding: 13px 12px; }
    .menu.open a::after { display: none; }
    .posts-grid { grid-template-columns: 1fr; }
    .affiliate__text { padding: 38px 28px; }
    .footer-grid { grid-template-columns: 1fr 1fr; gap: 28px; }
    .footer-brand { grid-column: 1 / -1; }
    .hero { padding: 36px 0 44px; }
  }
  @media (max-width: 460px) {
    .footer-grid { grid-template-columns: 1fr; }
    .hero__actions .btn, .affiliate__actions .btn { flex: 1; justify-content: center; }
  }

  @media (prefers-reduced-motion: reduce) {
    * { transition: none !important; scroll-behavior: auto; }
  }

  #tweaks-root { position: fixed; z-index: 90; }
</style>
</head>
<body>

  <template id="__bundler_thumbnail">
    <svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
      <rect width="100" height="100" fill="#21392b"/>
      <circle cx="50" cy="50" r="22" fill="none" stroke="#d4ad5c" stroke-width="4"/>
      <circle cx="50" cy="50" r="13" fill="none" stroke="#b58f33" stroke-width="2"/>
    </svg>
  </template>

  <!-- ===================== HEADER ===================== -->
  <header class="site-header" id="header">
    <div class="wrap nav">
      <a href="#topo" class="brand" aria-label="O Um Blog — início">
        <span class="brand__ring" aria-hidden="true"></span>
        <span class="brand__txt">
          <span class="brand__name">O Um Blog</span>
          <span class="brand__sub">Crônicas da Terra-média</span>
        </span>
      </a>

      <nav class="menu" id="menu">
        <a href="#topo" class="active">Home</a>
        <a href="#leituras">Guias de Leitura</a>
        <a href="#lore">Lore</a>
        <a href="#contato">Contato</a>
      </nav>

      <form class="search" role="search" onsubmit="return false;">
        <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round"><circle cx="11" cy="11" r="7"></circle><path d="m21 21-4.3-4.3"></path></svg>
        <input type="search" placeholder="Buscar na Terra-média…" aria-label="Buscar" />
      </form>

      <button class="hamburger" id="hamburger" aria-label="Abrir menu" aria-expanded="false">
        <span></span><span></span><span></span>
      </button>
    </div>
  </header>

  <main id="topo">
    <!-- ===================== HERO ===================== -->
    <section class="hero">
      <div class="wrap hero__grid">
        <div class="hero__content">
          <div class="hero__eyebrow"><span class="eyebrow">Guia Essencial · Leitura</span></div>
          <h1>Por onde começar a ler <em>Tolkien?</em> A Ordem Certa</h1>
          <p class="hero__dek" style="font-weight: 700">Do Silmarillion ao O Senhor dos Anéis  — um roteiro definitivo para atravessar a Terra-média sem se perder pelos caminhos, na sequência que mais encanta novos leitores.</p>
          <div class="hero__meta">
            <span>12 min de leitura</span><span class="dot"></span>
            <span>Atualizado em Junho de 2026</span><span class="dot"></span>
            <span>por Mithrandir</span>
          </div>
          <div class="hero__actions">
            <a href="#leituras" class="btn btn--gold">
              Ler Artigo
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"></path><path d="m13 6 6 6-6 6"></path></svg>
            </a>
            <a href="#lore" class="btn btn--ghost">Ver todos os guias</a>
          </div>
        </div>
        <div class="hero__media ph" role="img" aria-label="Imagem de destaque do artigo">
          <div class="spot spot--1" aria-hidden="true"></div>
          <div class="spot spot--2" aria-hidden="true"></div>
          <div class="spot spot--3" aria-hidden="true"></div>
          <div class="hero__badge"><span class="star">★</span> Mais lido da semana</div>
          <span class="ph__tag">arte do hero · mapa da terra-média</span>
        </div>
      </div>
    </section>

    <!-- ===================== AD 1 ===================== -->
    <div class="wrap">
      <div class="ad-block ad-block--leaderboard" aria-label="Espaço publicitário">
        <div class="ad-block__label">Publicidade</div>
        <div class="ad-block__hint">Google AdSense · banner responsivo (970 × 250)</div>
      </div>
    </div>

    <!-- ===================== POSTS ===================== -->
    <section class="posts" id="leituras">
      <div class="wrap">
        <div class="section-head">
          <div>
            <span class="eyebrow">Da biblioteca</span>
            <h2 style="margin-top:10px;">Leituras Recentes</h2>
            <p>Lore, história e guias de compra para o fã exigente.</p>
          </div>
          <a href="#" class="more">Ver todos os artigos
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"></path><path d="m13 6 6 6-6 6"></path></svg>
          </a>
        </div>

        <div class="posts-grid">
          <!-- Card 1 -->
          <article class="card">
            <div class="card__media ph"><span class="ph__tag">ilustração · númenor</span></div>
            <div class="card__body">
              <span class="tag">Lore</span>
              <h3>A Queda de Númenor: a Atlântida de Tolkien</h3>
              <p class="card__excerpt">Como a maior civilização dos Homens afundou no orgulho — e por que sua sombra ainda paira sobre Gondor.</p>
              <div class="card__foot">
                <span>8 min</span>
                <span class="card__more">Ler mais
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"></path><path d="m13 6 6 6-6 6"></path></svg>
                </span>
              </div>
            </div>
          </article>

          <!-- Card 2 -->
          <article class="card">
            <div class="card__media ph"><span class="ph__tag">capas · box set</span></div>
            <div class="card__body">
              <span class="tag tag--gold">Guia de Compra</span>
              <h3>As 5 Melhores Edições de O Senhor dos Anéis em Português</h3>
              <p class="card__excerpt">Capa dura, ilustradas e de bolso comparadas lado a lado: qual vale o investimento da sua estante?</p>
              <div class="card__foot">
                <span>11 min</span>
                <span class="card__more">Ler mais
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"></path><path d="m13 6 6 6-6 6"></path></svg>
                </span>
              </div>
            </div>
          </article>

          <!-- Card 3 -->
          <article class="card">
            <div class="card__media ph"><span class="ph__tag">ilustração · floresta velha</span></div>
            <div class="card__body">
              <span class="tag">Lore</span>
              <h3>Quem é Tom Bombadil? A maior charada da Terra-média</h3>
              <p class="card__excerpt">Nem o Anel tem poder sobre ele. Reunimos as teorias mais fascinantes sobre o enigma favorito dos fãs.</p>
              <div class="card__foot">
                <span>6 min</span>
                <span class="card__more">Ler mais
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"></path><path d="m13 6 6 6-6 6"></path></svg>
                </span>
              </div>
            </div>
          </article>
        </div>
      </div>
    </section>

    <!-- ===================== AFFILIATE ===================== -->
    <section class="affiliate" id="lore">
      <div class="wrap">
        <div class="affiliate__inner">
          <div class="affiliate__text">
            <div class="affiliate__eyebrow">Para o colecionador</div>
            <h2>Edições de Luxo de <em>Colecionador</em></h2>
            <p>Encadernação em couro, mapas dobráveis e as ilustrações do próprio Tolkien. As edições definitivas para quem quer guardar a Terra-média na estante por gerações.</p>
            <div class="affiliate__perks">
              <span>Capa em couro</span>
              <span>Ilustrações originais</span>
              <span>Caixa de presente</span>
              <span>Edição limitada</span>
            </div>
            <div class="affiliate__actions">
              <a href="#" class="btn btn--gold" rel="sponsored nofollow" target="_blank">
                Ver na Amazon
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M7 17 17 7"></path><path d="M7 7h10v10"></path></svg>
              </a>
              <span class="affiliate__disclosure">Contém link de afiliado · você não paga nada a mais</span>
            </div>
          </div>
          <div class="affiliate__media ph" role="img" aria-label="Foto do box set de colecionador">
            <span class="ph__tag">foto · box set de luxo</span>
          </div>
        </div>
      </div>
    </section>

    <!-- ===================== AD 2 (inline, discreto) ===================== -->
    <div class="wrap" style="padding-bottom:56px;">
      <div class="ad-block ad-block--inline" aria-label="Espaço publicitário">
        <div class="ad-block__label">Publicidade</div>
        <div class="ad-block__hint">Google AdSense · in-feed (728 × 90)</div>
      </div>
    </div>
  </main>

  <!-- ===================== FOOTER ===================== -->
  <footer class="site-footer" id="contato">
    <div class="wrap">
      <div class="footer-grid">
        <div class="footer-brand">
          <a href="#topo" class="brand">
            <span class="brand__ring" aria-hidden="true"></span>
            <span class="brand__txt">
              <span class="brand__name">O Um Blog</span>
              <span class="brand__sub">Crônicas da Terra-média</span>
            </span>
          </a>
          <p>Um blog feito por fãs, para fãs. Lore, guias de leitura e as melhores edições para mergulhar na obra de J.R.R. Tolkien.</p>
          <div class="socials">
            <a href="#" aria-label="Instagram"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="5"></rect><circle cx="12" cy="12" r="4"></circle><circle cx="17.5" cy="6.5" r="1" fill="currentColor" stroke="none"></circle></svg></a>
            <a href="#" aria-label="YouTube"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2.5" y="5" width="19" height="14" rx="4"></rect><path d="m10 9 5 3-5 3z" fill="currentColor" stroke="none"></path></svg></a>
            <a href="#" aria-label="X"><svg viewBox="0 0 24 24" fill="currentColor" stroke="none"><path d="M17.5 3h3l-7 8 8.2 10h-6.4l-5-6.3L8 21H5l7.4-8.5L4.5 3h6.5l4.5 5.8z"></path></svg></a>
            <a href="#" aria-label="Newsletter"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="5" width="18" height="14" rx="3"></rect><path d="m4 7 8 6 8-6"></path></svg></a>
          </div>
        </div>
        <div class="footer-col">
          <h4>Navegação</h4>
          <ul>
            <li><a href="#topo">Home</a></li>
            <li><a href="#leituras">Guias de Leitura</a></li>
            <li><a href="#lore">Lore</a></li>
            <li><a href="#contato">Contato</a></li>
          </ul>
        </div>
        <div class="footer-col">
          <h4>Categorias</h4>
          <ul>
            <li><a href="#">A Terra-média</a></li>
            <li><a href="#">Personagens</a></li>
            <li><a href="#">Edições & Livros</a></li>
            <li><a href="#">Filmes & Séries</a></li>
          </ul>
        </div>
        <div class="footer-col">
          <h4>Institucional</h4>
          <ul>
            <li><a href="#">Sobre o blog</a></li>
            <li><a href="#">Política de Privacidade</a></li>
            <li><a href="#">Divulgação de Afiliados</a></li>
            <li><a href="#">Anuncie aqui</a></li>
          </ul>
        </div>
      </div>
      <div class="footer-bottom">
        <span>© 2026 O Um Blog · Todos os direitos reservados.</span>
        <span class="legal">Site independente feito por fãs. Não é afiliado, autorizado ou patrocinado por Tolkien Estate ou Middle-earth Enterprises.</span>
      </div>
    </div>
  </footer>

  <div id="tweaks-root"></div>

  <!-- ===================== INTERAÇÕES ===================== -->
  <script>
    (function () {
      var header = document.getElementById('header');
      var onScroll = function () {
        if (window.scrollY > 12) header.classList.add('scrolled');
        else header.classList.remove('scrolled');
      };
      window.addEventListener('scroll', onScroll, { passive: true });
      onScroll();

      var ham = document.getElementById('hamburger');
      var menu = document.getElementById('menu');
      ham.addEventListener('click', function () {
        var open = menu.classList.toggle('open');
        ham.setAttribute('aria-expanded', open ? 'true' : 'false');
      });
      menu.querySelectorAll('a').forEach(function (a) {
        a.addEventListener('click', function () {
          menu.classList.remove('open');
          ham.setAttribute('aria-expanded', 'false');
          menu.querySelectorAll('a').forEach(function (x) { x.classList.remove('active'); });
          a.classList.add('active');
        });
      });

      // Scroll spy
      var sections = ['topo', 'leituras', 'lore', 'contato'];
      var links = {};
      menu.querySelectorAll('a').forEach(function (a) {
        var id = a.getAttribute('href').replace('#', '');
        links[id] = a;
      });
      var spy = function () {
        var pos = window.scrollY + 130;
        var current = 'topo';
        sections.forEach(function (id) {
          var el = document.getElementById(id);
          if (el && el.offsetTop <= pos) current = id;
        });
        Object.keys(links).forEach(function (id) {
          links[id].classList.toggle('active', id === current);
        });
      };
      window.addEventListener('scroll', spy, { passive: true });
      spy();
    })();
  </script>

  <!-- ===================== TWEAKS (React) ===================== -->
  <script src="https://unpkg.com/react@18.3.1/umd/react.development.js" integrity="sha384-hD6/rw4ppMLGNu3tX5cjIb+uRZ7UkRJ6BPkLpg4hAu/6onKUg4lLsHAs9EBPT82L" crossorigin="anonymous"></script>
  <script src="https://unpkg.com/react-dom@18.3.1/umd/react-dom.development.js" integrity="sha384-u6aeetuaXnQ38mYT8rp6sbXaQe3NL9t+IBXmnYxwkUI2Hw4bsp2Wvmx4yRQF1uAm" crossorigin="anonymous"></script>
  <script src="https://unpkg.com/@babel/standalone@7.29.0/babel.min.js" integrity="sha384-m08KidiNqLdpJqLq95G/LEi8Qvjl/xUYll3QILypMoQ65QorJ9Lvtp2RXYGBFj1y" crossorigin="anonymous"></script>
  <script type="text/babel" src="tweaks-panel.jsx"></script>
  <script type="text/babel">
    const TWEAK_DEFAULTS = /*EDITMODE-BEGIN*/{
      "atmosfera": "Pergaminho",
      "cards": "Clássico",
      "anuncios": true
    }/*EDITMODE-END*/;

    function TweakApp() {
      const [t, setTweak] = useTweaks(TWEAK_DEFAULTS);
      React.useEffect(() => {
        const r = document.documentElement;
        r.dataset.theme = t.atmosfera === 'Noturno' ? 'noturno' : 'parchment';
        r.dataset.card = t.cards === 'Pergaminho' ? 'pergaminho' : 'classico';
        r.dataset.ads = t.anuncios ? 'on' : 'off';
        try {
          localStorage.setItem('oub-theme', r.dataset.theme);
          localStorage.setItem('oub-card', r.dataset.card);
          localStorage.setItem('oub-ads', r.dataset.ads);
        } catch (e) {}
      }, [t]);

      return (
        <TweaksPanel title="Tweaks">
          <TweakSection label="Atmosfera" />
          <TweakRadio label="Tema" value={t.atmosfera}
            options={['Pergaminho', 'Noturno']}
            onChange={(v) => setTweak('atmosfera', v)} />
          <TweakSection label="Artigos" />
          <TweakRadio label="Estilo dos cards" value={t.cards}
            options={['Clássico', 'Pergaminho']}
            onChange={(v) => setTweak('cards', v)} />
          <TweakSection label="Monetização" />
          <TweakToggle label="Mostrar blocos de anúncio" value={t.anuncios}
            onChange={(v) => setTweak('anuncios', v)} />
        </TweaksPanel>
      );
    }

    ReactDOM.createRoot(document.getElementById('tweaks-root')).render(<TweakApp />);
  </script>
</body>
</html>
